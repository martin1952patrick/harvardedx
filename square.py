# we can import a function e.g.

# from functions (module) import square(function) and use it here

# for i in range (12):
#     print(f"square of {i} is {square(i)}")


# or
# We can import the whole module and use the function e.g.

import functions

for i in range (12):
     print(f"square of {i} is {functions.square(i)}")
